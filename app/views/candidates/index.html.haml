/ Modal
#donation-modal.modal.fade{"aria-labelledby" => "donation-modalLabel", :role => "dialog", :tabindex => "-1"}
  .modal-dialog{:role => "document"}
    .modal-content
      .modal-header
        %button.close{"aria-label" => "Close", "data-dismiss" => "modal", :type => "button"}
          %span{"aria-hidden" => "true"} ×
        %h4#donation-modal-abel.modal-title Donations Are Better When Tweeted
      .modal-body
        - @tweet = Tweet.new
        = form_for @tweet do |f|
          .form-group
            = f.label "Tweet Text"

            -# TODO look into twitter display plugin that will also allow for url relaxing and nice formatting
            = f.text_area :text, {class: "form-control", maxlength: "140", rows: "2"}

            = hidden_field_tag 'tweet[donation_id]', 'placholder_id', {id: 'donation-id-input'}
            -# TODO troubleshoot for safari
            = f.submit 'Submit', {class: "hidden"}
        -# %form#tweet-send
        -#   .form-group
        -#     %label{:for => "exampleInputEmail1"} Email address
        -#     %input#exampleInputEmail1.form-control{:placeholder => "Email", :type => "email"}/
        -#   .form-group
        -#     %label{:for => "exampleInputPassword1"} Password
        -#     %input#exampleInputPassword1.form-control{:placeholder => "Password", :type => "password"}/
        -#   .form-group
        -#     %label{:for => "exampleInputFile"} File input
        -#     %input#exampleInputFile{:type => "file"}/
        -#     %p.help-block Example block-level help text here.
        -#   .form-group
        -#     %label{:for => "tweet-text"} Tweet text
        -#     %textarea#tweet-text.form-control{rows: 3}

          .checkbox
            %label
              %input{:type => "checkbox"}/
              Check me out
          -# %button.btn.btn-default{:type => "submit"} Submit

      .modal-footer
        %button.btn.btn-default{"data-dismiss" => "modal", :type => "button"} Close
        %button.btn.btn-primary#donation-dummy-submit{:type => "button"} Tweet!

.container-fluid
  .row
    .col-xs-12{style: "text-align: center;"}
      %img.congress-pic{src: 'assets/US_Capitol_from_NW.jpg'}
    }
    .col-xs-12
      %h2 114th Congress
      %table.table.table-hover.candidates{id: 'candidates-table'}
        %caption Show me the Money
        %thead
          %tr
            %th
            %th Last Name
            %th First Name
            %th Chamber
            %th Party
            %th State
            %th District
            %th Twitter Handle
        %tbody
          - @candidates.each_with_index do |candidate, index|
            %tr.main
              %td.expand-donations{data: {candidate_id: candidate.id}} +
              %th{:scope => "row"}= candidate.last_name
              %td= candidate.first_name
              %td= candidate.chamber
              %td= candidate.party
              %td= candidate.state
              %td= candidate.district
              %td= candidate.twitter_handle.present? ? "@" + candidate.twitter_handle : ""
            -# %tr.hidden-row
            -#   %td.hidden-row{colspan: "7"}
            -#     .accordion-body.collapse{id: "candidate-donations-#{index}", data: {candidate_id: candidate.id} }
              
      -# my template donation table which I clone in JS
      %table.table.table-striped.table-hover.donations.hidden
        %thead
          %tr
            %th Amount
            %th Date
            %th Last Name
            %th First Name
            %th Employer
            %th Occupation
            %th State
            %th City
            %th FEC Donation ID
            %th Tweet
        %tbody
          
